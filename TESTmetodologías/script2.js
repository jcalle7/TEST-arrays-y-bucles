function testBucle() {
    let contador = 0;

    while (contador < 5) {
        alert("Iteración " + (contador + 1));
        contador++;
    }
}
    contador = 0;

    do {
        alert("Otra iteración " + (contador + 1));
        contador++;
    } while (contador < 3);

testBucle();